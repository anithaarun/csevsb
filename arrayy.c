#include<stdio.h>
void main()
{
int i,a[100],n;
{
printf("enter the number of elements to store in the array");
scanf("%d",&n);
 printf("number of elemente in the array\n);
 for(i=0;i<n;i++)
 {
 printf("element-%d",&a[i]);
 }
 printf("the values stored in the array are \n");
 for(i=0;i<n;i++)
 {
 printf("%5d",&a[i]);
 }
 }

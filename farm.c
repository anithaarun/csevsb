#include<stdio.h>
int main()
{
float l,b,area;
printf("\nenter the length of the farm:");
scanf("%f",&l);
printf("\nenter the breadth of the farm:");
scanf("%f",&b);
area=l*b;
printf("\nthe area is %f",area);
return 0;
}

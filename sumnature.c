#include<stdio.h>
int main()
{
    int n,sum=0,i;
    printf("enter the number");
    scanf("%d",&n);
    for(i=0;i<=n;i++)
    {
        sum=sum+i;
    }
    printf("the sum is%d sum");
    return 0;
}
